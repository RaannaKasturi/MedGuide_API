services:
  # A Docker web service
  - type: web
    name: MedGuide-API
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 127.0.0.1 --port 8000
